# 🛒 SauceDemo Automated Shopping Tests

## Тестове завдання 

1. Напишіть сценарії автоматичного тестування для сортування за ціною і додавання товару у кошик на сайті  https://www.saucedemo.com/. 
2. До ваших тестів додайте документацію з налаштуваннями та розмістіть ваше рішення на GitHub.

Цей репозиторій містить автоматизовані тести на Python з використанням Selenium та Pytest для перевірки базового функціоналу інтернет-магазину (сортування та кошик) на демо-сайті Sauce Labs.

## 🚀 Інструкція з Налаштування та Запуску

### 1. Передумови

* Встановлений **Python 3.8+**.
* Встановлений **Chrome** (браузер).
* **ChromeDriver** (відповідна версія для вашого Chrome) має бути доступний у системній змінній `PATH`.

### 2. Клонування та Налаштування

1.  Клонуйте репозиторій:
    ```bash
    git clone [ВСТАВТЕ ВАШ URL]
    cd SauceDemo_AutoTests
    ```
2.  Створіть та активуйте віртуальне середовище:
    ```bash
    python -m venv venv
    source ./venv/Scripts/activate
    ```
3.  Встановіть залежності:
    ```bash
    pip install -r requirements.txt
    ```

### 3. Запуск Тестів

Переконайтеся, що ви бачите префікс `(venv)` у терміналі, і запустіть:

```bash
pytest -v
